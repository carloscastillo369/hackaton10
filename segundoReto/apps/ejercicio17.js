/*
let number=[10, 4, 1, 4, -10, -50, 32, 21];

function diffMaxMin(a){
    let b=Math.min.apply(null,a);
    let c=Math.max.apply(null,a);
    let d=b-c;
    return d;
}

console.log(diffMaxMin(number));
*/